{
  "name": "scrubber",
  "private": true,
  "version": "0.2.0",
  "main": "build/index.js",
  "dependencies": {
    "traverse": "^0.6.6"
  },
  "scripts": {
    "prepublish": "rm -fr build && mkdir -p build && coffee -p -b index.coffee > build/index.js"
  }
}
